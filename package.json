{
  "name": "entry-io-mcp",
  "version": "1.0.0",
  "description": "MCP server cho entry.io.vn — agent (Claude…) tự dùng tunnel / DDNS / SSH key / subdomain / VPN của entry.io.vn",
  "type": "module",
  "bin": { "entry-mcp": "src/index.mjs" },
  "files": ["src", "README.md"],
  "engines": { "node": ">=18" },
  "scripts": { "start": "node src/index.mjs" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
